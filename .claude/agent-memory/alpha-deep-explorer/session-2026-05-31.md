# Alpha Deep Explorer Session - 2026-05-31

## Status
- Total alphas: 65
- Already submitted: 6 (Sharpe: 2.50, 2.48, 2.47, 2.44, 1.75, 1.31)
- Unsubmitted but submittable: 15 (all blocked by PROD_CORRELATION)

## Critical Issue
**PROD_CORRELATION blocks all new submissions**

All remaining submittable alphas use vec_max(min_loan_rate) or vec_max(mean_loan_rate) pattern which is highly correlated with already submitted alphas.

## Exploration Attempted

### 1. New shortinterest3 VECTOR Fields
Explored: loan_rate_volatility, loan_utilization_ratio, borrow_activity_score

Error received: `Operator ts_max does not support event inputs`

Solution needed: Use ts_backfill() wrapper, but that has its own issues:
- Error: `Required attribute "lookback" must have a value`

### 2. Alternative Datasets
Checked:
- analyst4: MATRIX fields only (50 total)
- analyst10: MATRIX fields only (50 total)
- analyst14: MATRIX fields only
- biasfree_analyst: 8 VECTOR fields
- pv48: 0 fields accessible
- risk60: 5 VECTOR fields (rsk60_offer, rsk60_crowding, etc.)
- fcf10: 0 fields

### 3. API Performance Issue
Simulations are stalling at 35% progress and timing out (>10 min per simulation).

## Key Findings

1. **VECTOR fields in shortinterest3**: 31 total, but most haven't been tested
   - Working: min_loan_rate, mean_loan_rate, max_loan_rate, shrt3_bar, shrt3_utilizationpercent_units
   - Untested: loan_rate_volatility, loan_utilization_ratio, borrow_activity_score, average_loan_duration_days, loaned_share_count

2. **API Workaround Found**:
   - Using `platform_functions.BrainApiClient` + `SimulationData` works
   - But simulations take 10+ min and often stall

3. **CLI backtest works** but also times out for complex expressions

## Next Steps

1. **Wait for API stability** before attempting new submissions
2. **Try simple expressions** first to check if API is responsive
3. **Explore analyst4 MATRIX-based alphas** (Sharpe 1.58-2.21 observed in OS)
4. **Test different neutralization** on existing alphas (MARKET vs INDUSTRY vs SECTOR)

## Files
- Alpha state: /home/zxx/worldQuant/worldquant_brain/state/alphas.json
- Results cache: /tmp/multi_agent/results.json